
# #######################################################################################
# AUTOGENERATED HYPR CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#

# See https://wiki.hyprland.org/Configuring/Monitors/
#
# External Dell S2725QC: 4K @ 120Hz, main display when attached
monitor = DP-1, 3840x2160@120, 0x0, 1.67
# Laptop panel: 2880x1920 @ 120Hz, sits to the right of the external
monitor = eDP-1, 2880x1920@120, auto-left, 1.67
# Fallback for any other monitor
monitor = , preferred, auto, 1.67

# Workspaces 1-9 live on the external monitor when it's attached and fall
# back to the laptop panel when it's not; 10 stays on the laptop.
workspace = 1, monitor:DP-1, default:true
workspace = 2, monitor:DP-1, default:true
workspace = 3, monitor:DP-1, default:true
workspace = 4, monitor:DP-1, default:true
workspace = 5, monitor:DP-1, default:true
workspace = 6, monitor:DP-1, default:true
workspace = 7, monitor:DP-1, default:true
workspace = 8, monitor:DP-1, default:true
workspace = 9, monitor:DP-1, default:true
workspace = 10, monitor:eDP-1, default:true

exec-once = awww-daemon
exec-once = hypridle
exec-once = hyprsunset
exec-once = export PATH="~/.local/bin/:$PATH"
exec-once = hyprctl setcursor Bibata-Modern-Classic 24
# exec-once = swaybg -m fill -i /etc/nixos/misc/wallpaper.jpg
exec-once = waybar
exec-once = pactl set-sink-mute @DEFAULT_SINK@ true
# for pixel art use the following command to upscale
# ffmpeg -i small_pixel_art.gif -vf "scale=1920:1080:flags=neighbor,split[s0][s1];[s0]palettegen=max_colors=256:stats_mode=diff[p];[s1][p]paletteuse=dither=none" fullhd_output.gif
exec-once = awww img ~/.config/current-wallpaper

group {
    groupbar {
        col.active = rgb(${PURPLE}) rgb(${BG_ALT}) 90deg
        col.inactive = rgba(${BG}dd)
    }
}


animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}
# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
# exec-once = waybar & hyprpaper & firefox

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# Set programs that you use
exec-once = ghostty --gtk-single-instance=true --quit-after-last-window-closed=false --initial-window=false
$terminal = ghostty --gtk-single-instance=true

$menu = rofi --show drun

 bind = , xf86audioraisevolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +5%

bind = , xf86audiolowervolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -5%

bind = , XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle

bind = , XF86MonBrightnessUp, exec, brightnessctl set +10%

bind = , XF86MonBrightnessDown, exec, brightnessctl set 10%-

bind = , XF86Display, exec, slurp | grim -g- | wl-copy


# Hyprland cursor settings
env = XCURSOR_THEME,Bibata-Modern-Ice
env = XCURSOR_SIZE,24

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_options=ctrl:nocaps
    kb_layout = us
    kb_variant =
    kb_model =
    kb_rules =

    follow_mouse = 1

    touchpad {
        natural_scroll = no
    }

    sensitivity = 0 # -1.0 to 1.0, 0 means no modification.
}

general {
    col.active_border = rgb(${ACCENT})
    col.inactive_border = rgb(${BORDER})
    col.nogroup_border = rgba(${BG}dd)
    col.nogroup_border_active = rgb(${PURPLE}) rgb(${BG_ALT}) 90deg
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 4
    gaps_out = 4
    border_size = 1

    layout = dwindle

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false
}

decoration {

    rounding = 0

    blur {
        enabled = true
        size = 3
        passes = 3
    }

}


animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    preserve_split = yes # you probably want this
}




misc {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}

layerrule {
  name = layerrule-1
  blur = on
  ignore_alpha = 0
  match:namespace = waybar
}

layerrule {
  name = layerrule-2
  blur = on
  ignore_alpha = 0
  match:namespace = rofi
}

# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Return, exec, $terminal

bind = $mainMod,  Q, exec, rofi -show drun

bind = $mainMod SHIFT, P, exec, $HOME/scripts/rofi-power

bind = $mainMod, W, exec, $HOME/scripts/rofi-dotfiles

bind = $mainMod SHIFT,  Q, killactive,
bind = $mainMod, F, fullscreen
bind = $mainMod, E, exec, ghostty -e yazi
bind = $mainMod, R, exec, $menu
bind = $mainMod, O, exec, hyprlock
bind = $mainMod, N, exec, $HOME/scripts/night-mode
bind = $mainMod, M, exec, spotify-launcher
bind = $mainMod, B, exec, firefox
bind = $mainMod, V, exec, mullvad-vpn
bind = $mainMod, Y, exec, firefox youtube.com
bind = $mainMod, P, exec, firefox mail.proton.me

# Move focus with mainMod + arrow keys
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1


# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
